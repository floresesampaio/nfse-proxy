version: '3.8'

services:
  nfse-proxy:
    image: nfse-proxy
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - INTERNAL_TOKEN=${INTERNAL_TOKEN}
      - PORT=3001
    restart: unless-stopped
